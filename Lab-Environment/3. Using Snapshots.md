- **Snapshots** are saved states of a virtual machine at a specific point in time.
- A snapshot preserves the VM’s current configuration, installed software, and system state.
- Snapshots are useful because they let you return to a previously working state if the VM becomes corrupted or misconfigured.


1. Hash a whole folder in one line (H0): 

```bash
find /etc -type f -exec sha256sum {} \; 2>/dev/null | sha256sum 
```

- Hash Result

```
2236aed00dad85ff8aa7b8ab8939ebe2b8bbc4a4d781f663ecf37811c9a2760b
```

2. Take a snapshot (from the hypervisor, VM powered on)

![](/assets/3/img-1.png)

* Click on **Snapshots** -> **Current state** -> **Take**

![](/assets/3/img-2.png)

3. Start Kali normally & Use some changes:

```bash
echo "test" >> /etc/hostname
```

4. Hash a whole folder again (H1):

```bash
find /etc -type f -exec sha256sum {} \; 2>/dev/null | sha256sum 
```

![](/assets/3/img-3.png)

**Result**: Different Hash Value (H1 != H0)

5. Revert directly to the previous state

![](/assets/3/img-4.png)


- Uncheck the box for: **Create a snapshot of the current machine state**
- Then click: **Restore**

![](/assets/3/img-5.png)

6. Start VM & Calculate hash (H2):

![](/assets/3/img-6.png)

 ```
 2236aed00dad85ff8aa7b8ab8939ebe2b8bbc4a4d781f663ecf37811c9a2760b
 ```
 
- **Result**: Calculate hash  (Expected: H2 == H0)

